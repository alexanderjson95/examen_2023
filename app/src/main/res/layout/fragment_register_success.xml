<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@color/theme_one_backdrop"

    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp">
        <!-- MENU -->

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Registreringen lyckades!"
            android:textColor="@color/theme_one_activeBtn"
            style="@style/TextAppearance.Material3.TitleLarge.Emphasized"/>

        <LinearLayout
            android:id="@+id/user_form"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="32dp"
            android:elevation="4dp">
            <com.google.android.material.button.MaterialButton
                android:id="@+id/navLoginBtn"
                android:text="@string/nav_login_btn"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_marginTop="18dp"
                android:layout_marginStart="12dp"
                android:backgroundTint="@color/theme_one_inactiveBtn"
                android:textColor="@color/theme_one_activeBtn"
                android:layout_marginEnd="12dp"
                android:fontFamily="sans-serif"
                android:textStyle="bold" />
        </LinearLayout>

    </LinearLayout>
    <nl.dionsegijn.konfetti.xml.KonfettiView
        android:id="@+id/konfettiView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</FrameLayout>